{# templates/admin/fields/images.html.twig #}

{% for image in field.value %}
    <img src="{{ asset('uploads/images/pictures/' ~ image.path) }}" alt="{{ image.path }}" class="img-thumbnail" style="max-width: 100px;">
{% endfor %}